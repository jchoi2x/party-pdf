{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "lib/integrations/*",
    "scripts"
  ],
  "scripts": {
    "build": "bun run typecheck && bun run --cwd artifacts/mockup-sandbox build && bun run --cwd artifacts/pdf-collab build",
    "typecheck:libs": "tsc --build",
    "typecheck": "bun run typecheck:libs && bun run --cwd artifacts/mockup-sandbox typecheck && bun run --cwd artifacts/pdf-collab typecheck && bun run --cwd scripts typecheck"
  },
  "private": true,
  "packageManager": "bun@1.2.15",
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
